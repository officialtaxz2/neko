# Copy this file to .env on the deployment server and replace both passwords.
# .env is ignored by Git; never commit real credentials.

NEKO_IMAGE=my-neko/brave:latest
NEKO_HTTP_BIND=127.0.0.1
NEKO_HTTP_PORT=8082
NEKO_WEBRTC_EPR=51000-51100
NEKO_DESKTOP_SCREEN=1920x1080@30

NEKO_PROFILE_DIR=./files
NEKO_DOWNLOADS_DIR=./downloads
NEKO_POLICY_FILE=./apps/brave/policies.json

NEKO_MEMBER_MULTIUSER_USER_PASSWORD=
NEKO_MEMBER_MULTIUSER_ADMIN_PASSWORD=

# Optional. Generate without host OpenSSL through
# `docker compose -f docker-compose.validation.yaml run --rm token-generator`;
# leave empty to disable the server-enforced #/<16-character-token> view-only path.
NEKO_VIEW_ONLY_TOKEN=

# Set this when remote clients need an explicit reachable server address, then
# uncomment NEKO_WEBRTC_NAT1TO1 in docker-compose.yaml.
# NEKO_WEBRTC_NAT1TO1=203.0.113.10

# The opt-in adaptive-quality setup needs no additional environment value.
# Activate it by adding -f docker-compose.adaptive.yaml to Compose commands.

# Used only when docker-compose.webcodecs-ws.yaml is added explicitly.
# Keep the feature disabled by omitting that overlay. Set one exact public
# http(s) origin and only the immediate reverse-proxy address/CIDR that Neko
# actually sees. This deployment overlay intentionally accepts one of each.
NEKO_MEDIA_WEBCODECS_WS_ALLOWED_ORIGINS=
NEKO_MEDIA_WEBCODECS_WS_TRUSTED_PROXIES=
NEKO_MEDIA_WEBCODECS_WS_ALLOW_INSECURE_LOOPBACK=false
NEKO_MEDIA_WEBCODECS_WS_MAX_CONNECTIONS=128
